import 'package:flutter/material.dart';

class SearchLove extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purpleAccent,
    );
  }

}